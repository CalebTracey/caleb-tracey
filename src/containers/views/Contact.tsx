import React, { FC } from 'react'
import Header from '../../components/Header'

const Contact: FC = () => {
    return (
        <div id="contact" className="view" style={{ top: '400vh' }}>
            <Header title="Get in touch" />
        </div>
    )
}

export default Contact
