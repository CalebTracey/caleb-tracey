import React, { FC } from 'react'
import Header from '../../components/Header'

const Portfolio: FC = () => {
    return (
        <div id="portfolio" className="view" style={{ top: '300vh' }}>
            <Header title="Stuff I've built" />
        </div>
    )
}

export default Portfolio
