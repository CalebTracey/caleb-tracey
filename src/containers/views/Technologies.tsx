import React, { FC } from 'react'
import Header from '../../components/Header'

const Technologies: FC = () => {
    return (
        <div id="technologies" className="view" style={{ top: '200vh' }}>
            <Header title="What I've been using" />
        </div>
    )
}

export default Technologies
