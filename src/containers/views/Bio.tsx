import React, { FC } from 'react'
import Header from '../../components/Header'

const Bio: FC = () => {
    return (
        <div id="bio" className="view" style={{ top: '100vh' }}>
            <Header title="A little about myself" />
        </div>
    )
}

export default Bio
